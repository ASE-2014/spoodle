<h1>Invitation#new</h1>
<p>Invite a new user to the event.</p>

<%= form_for [@event, @invitation] do |f| %>
    <%= f.collection_select :user_id, @users, :id, :username %>

    <%= link_to 'Cancel', @event %>
    <%= f.submit 'Create' %>
<% end %>
