<h1>Event#show</h1>
<p>Look at that damn nice event!</p>

<p>
    <strong>Title:</strong>
    <%= @event.title %>
</p>

<p>
    <strong>Description:</strong>
    <%= @event.description %>
</p>

<h2>Dates</h2>
<%= render @event.spoodle_dates %>

<h2>Invitations</h2>
<%= render @event.invitations %>
<%= @event.invitations.count %>

<%= link_to 'Edit Event', edit_event_path(@event) %> |
<%= link_to 'Back to Events', events_path %>